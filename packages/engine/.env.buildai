# BuildAI Engine Environment Variables
# These environment variables disable unnecessary engine components
# at the process level, complementing buildai.config.json5.

# ============================================================
# DISABLED COMPONENTS
# ============================================================

# Skip browser automation server (not needed for construction PM)
CLAWDBOT_SKIP_BROWSER_CONTROL_SERVER=1

# Skip canvas/A2UI host (not needed)
CLAWDBOT_SKIP_CANVAS_HOST=1

# Skip Gmail watcher hooks (not needed in MVP)
CLAWDBOT_SKIP_GMAIL_WATCHER=1

# Skip all messaging channel providers (Telegram, Discord, etc.)
# Web channel is handled separately via gateway WebSocket
CLAWDBOT_SKIP_CHANNELS=1

# ============================================================
# KEPT COMPONENTS (these run by default, listed for documentation)
# ============================================================

# Gateway WebSocket server — ENABLED (core of BuildAI)
# Memory system — ENABLED (file-based, no env needed)
# Compaction — ENABLED (built into agent runtime)
# Heartbeat — ENABLED (scheduled monitoring)
# Cron — ENABLED (scheduled jobs)
# Agent engine — ENABLED (LLM + tool dispatch)
# Tool system — ENABLED (exec-based tools)
# Webchat — ENABLED (web channel via gateway WebSocket)

# ============================================================
# CONFIGURATION
# ============================================================

# Point to BuildAI config instead of default clawdbot.json
# CLAWDBOT_CONFIG_PATH=./buildai.config.json5

# State directory for sessions, logs, caches
# CLAWDBOT_STATE_DIR=../../workspaces
