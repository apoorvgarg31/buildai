# BuildAI — Environment Variables
# Copy to .env.local and fill in real values. Never commit .env files.

# --- Gemini LLM (required) ---
GEMINI_API_KEY=

# --- PostgreSQL (pick ONE approach) ---

# Option A: Connection string (preferred for hosted databases)
# DATABASE_URL=postgresql://user:password@host:5432/buildai_demo

# Option B: Individual vars (for local / Unix socket setups)
DB_HOST=/var/run/postgresql
DB_PORT=5432
DB_NAME=buildai_demo
DB_USER=
# DB_PASSWORD=        # omit for peer auth
